# Copy to .env and fill real values — never commit .env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/muleguard
PGHOST=localhost
PGPORT=5432
PGDATABASE=muleguard
PGUSER=postgres
PGPASSWORD=postgres
BACKEND_PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
